<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="apply">
  
  <!-- 훈련사 지원 조회 -->
  <select id="list" resultType="applyDto">
  	select*from apply
  </select>
  <!-- 훈련사 지원 번호 -->
  <select id="one" parameterType="int" resultType="ApplyDto">
  	select*from apply where apply_no=#{applyNo}
  </select>
  <!-- 훈련사 아이디 상세 -->
   <select id="two" parameterType="string" resultType="ApplyDto">
  	select*from apply where member_id=#{memberId}
  </select>
  
 
  
  
  
  <!-- 훈련사 지원 상태가 승인으로 변환 -->
    <select id="approval" parameterType="ApplyDto">
    update apply set apply_status=#{applyStatus} where member_id=#{memberId};
    </select>
   <!-- 훈련사 지원 상태가 반려 변환 -->
     <select id="reject" parameterType="ApplyDto" >
    update apply set apply_status='반려' where apply_no=#{applyNo};
    </select>
  
  
  </mapper>