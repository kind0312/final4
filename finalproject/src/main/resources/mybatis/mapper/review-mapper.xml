<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="review">
	
	<!-- 리뷰 목록 조회 -->
	<select id="list" resultType="ReviewVO">
		select * from review_img_view order by review_writetime desc
	</select>
	
	<!-- 이용후기에서 이미지 불러오기 -->
	<select id="reviewList" resultType="ReviewVO" parameterType="int">
		select * from review_img_view where training_no = #{trainingNo} and pet_no = #{petNo};
	</select>
	
</mapper>